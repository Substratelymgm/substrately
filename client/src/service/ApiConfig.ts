const baseUrl = 'http://localhost:3000/'


const ApiConfig = {
    note: `${baseUrl}notes`,
    auth: `${baseUrl}auth`,
    article:`${baseUrl}articles`,
    link:`${baseUrl}links`,
    book:`${baseUrl}books`,
    user:`${baseUrl}users`,
    workSpace:`${baseUrl}workspaces`,
    media:`${baseUrl}media`
    
}

export default ApiConfig